# Build production (Vite) — données live, pas de fallback démo silencieux
VITE_STRICT_LIVE=true
VITE_API_BASE=/api
# Socket.IO : même origine que le site (nginx proxy /socket.io/)
VITE_SOCKET_URL=
# Service ML FastAPI via nginx /fastapi/
VITE_ML_API_BASE=/fastapi
