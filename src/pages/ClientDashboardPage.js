import React from 'react';
import ClientProductsPage from './ClientProductsPage';

/** Accueil client = catalogue produits (images, promos, commande, vendeurs). */
const ClientDashboardPage = () => <ClientProductsPage variant="home" />;

export default ClientDashboardPage;
