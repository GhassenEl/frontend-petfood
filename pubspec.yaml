name: aihub_assistant
description: "AIHub — Assistant IA : chat, Whisper, OCR, images DALL-E, traduction, TTS OpenAI."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.6.0
  file_picker: ^8.1.7
  record: ^5.2.1
  audioplayers: ^6.1.0
  path_provider: ^2.1.6
  intl: ^0.20.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
